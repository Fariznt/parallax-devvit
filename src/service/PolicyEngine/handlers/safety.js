export function evalSafety({ evalState, policyNode, negate, nodeAddress, evalNode, doEarlyExit, text, imageUrl, history,
// models,
// apiKeys,
 }) {
    console.warn('Safety nodes are not implemented');
}
